import { Box, Button, Dialog, DialogContent } from "@mui/material";
import { AddCircleOutlineRounded, DirectionsBusRounded, GroupsRounded, PaymentsRounded, SchoolRounded, SearchOffRounded, VisibilityRounded } from "@mui/icons-material";
import { useEffect, useMemo, useState } from "react";
import { useForm } from "react-hook-form";
import { toast } from "react-toastify";
import { enrollBus, unenrollBus } from "@/APIs/financials/bus";
import ClassFilter from "@/components/Filters/ClassFilter";
import SelectFilter from "@/components/Filters/SelectFilter";
import Container from "@/components/Container/Container";
import Input from "@/components/Input/Input";
import PaginationControls from "@/components/Pagination";
import Select from "@/components/Select/Select";
import Table from "@/components/Table/Table";
import { DialogHeader, EmptyState, FilterCard, FinancialHeader, FormActions, SectionCard, StatCard, StatsGrid, formFieldsSx } from "@/components/financial/FinancialShell";
import Years from "@/utils/constants/Years";
import { translateGender } from "@/utils/helpers/translateGender";
import { formatMoney, getErrorMessage, mapBusServiceType, mapFeeStatus } from "@/utils/financial/financialUtils";
import { useInstallmentPlans } from "@/utils/hooks/apis/financials/useInstallmentPlans";
import { useBusCandidates, useBusList } from "@/utils/hooks/apis/financials/useBus";
import usePermissions from "@/utils/hooks/usePermissions";
const headers=["اسم الطالب","السنة الدراسية","الفصل","نوع الخدمة","حالة الباص","إجمالي الرسوم","المدفوع","المتبقي"], body=["studentName","academicYear","className","serviceType","status","fee","totalPaid","remaining"], arr=v=>Array.isArray(v)?v:[];
const mapRow=item=>{const student=item?.student||{},cls=item?.class||{},bus=item?.bus||{},fee=Number(bus?.discount?bus?.netFee:bus?.fee||0),paid=Number(bus?.totalPaid||0),remaining=Math.max(fee-paid,0);return{id:student?._id||student?.id,studentName:student?.name||"—",academicYear:item?.academicYear||cls?.academicYear||"—",className:cls?.roomNumber?`${cls.roomNumber} - ${translateGender(cls?.gender,"class")}`:"—",serviceType:mapBusServiceType(bus?.serviceType),status:mapFeeStatus(bus?.status),fee:formatMoney(fee),totalPaid:formatMoney(paid),remaining:formatMoney(remaining),remainingRaw:remaining}};
const BusListPage=()=>{
 const [page,setPage]=useState(1),[limit,setLimit]=useState(10),[year,setYear]=useState(""),[classId,setClassId]=useState(""),[open,setOpen]=useState(false),[actionLoading,setActionLoading]=useState(false);
 const permissions=usePermissions("financial"),{installmentPlans=[]}=useInstallmentPlans();
 const filters=useMemo(()=>({academicYear:year||undefined,classId:classId||undefined,page,limit}),[year,classId,page,limit]), candidateFilters=useMemo(()=>({academicYear:year||undefined,classId:classId||undefined,limit:500}),[year,classId]);
 const {busRecords,loading,pagination,refetch}=useBusList(filters),{candidates,refetch:refetchCandidates}=useBusCandidates(candidateFilters);
 useEffect(()=>setClassId(""),[year]);useEffect(()=>setPage(1),[limit,year,classId]);
 const rows=useMemo(()=>arr(busRecords).map(mapRow),[busRecords]), remaining=rows.reduce((s,i)=>s+i.remainingRaw,0), paidCount=rows.filter(i=>i.remainingRaw===0).length;
 const plans=useMemo(()=>arr(installmentPlans).map(p=>({...p,displayName:`${p.name} (${p.numberOfInstallments} قسط)${p.isDefault?" - افتراضية":""}`})),[installmentPlans]);
 const candidateOptions=useMemo(()=>arr(candidates).map(i=>{const s=i?.student||{},c=i?.class||{};return{_id:s?._id||s?.id,displayName:`${s?.name||"طالب"} (${c?.roomNumber?`${c.roomNumber} - ${translateGender(c.gender,"class")}`:"بدون فصل"})`}}).filter(i=>i._id),[candidates]);
 const refresh=()=>Promise.all([refetch(),refetchCandidates()]);
 const unenroll=async(id,setActive)=>{setActionLoading(true);const r=await unenrollBus(id);if(r?.status){toast.success(r.message||"تم إلغاء التسجيل من الباص");setActive(false);await refresh()}else toast.error(getErrorMessage(r,"حدث خطأ أثناء إلغاء التسجيل"));setActionLoading(false)};
 const enroll=async d=>{setActionLoading(true);const r=await enrollBus(d.studentId,{fee:Number(d.fee),serviceType:d.serviceType,installmentPlanId:d.installmentPlanId||undefined});if(r?.status){toast.success(r.message||"تم تسجيل الطالب في خدمة الباص");setOpen(false);await refresh()}else toast.error(getErrorMessage(r,"حدث خطأ أثناء التسجيل"));setActionLoading(false)};
 const reset=()=>{setYear("");setClassId("");setPage(1)}, active=!!(year||classId);
 const action=permissions?.edit?<Button onClick={()=>setOpen(true)} variant="contained" startIcon={<AddCircleOutlineRounded/>} sx={{minHeight:42,borderRadius:"12px",background:"linear-gradient(135deg,var(--color-navy-light),var(--color-navy-dark))",fontWeight:800}}>إضافة طالب لخدمة الباص</Button>:null;
 return <Container><Box dir="rtl" sx={{pb:4}}><FinancialHeader title="إدارة خدمة الباص" description="سجّل الطلاب في خدمة الباص وتابع الرسوم والأقساط." count={pagination?.totalDocs??rows.length} actions={action}/><StatsGrid><StatCard label="إجمالي المشتركين" value={pagination?.totalDocs??rows.length} icon={<GroupsRounded/>}/><StatCard label="الظاهر في الصفحة" value={rows.length} icon={<VisibilityRounded/>}/><StatCard label="مكتملو السداد" value={paidCount} icon={<PaymentsRounded/>}/><StatCard label="المتبقي في الصفحة" value={formatMoney(remaining)} icon={<DirectionsBusRounded/>}/></StatsGrid><FilterCard title="تصفية الطلاب" description="حدّد السنة الدراسية والفصل." active={active} onReset={reset} columns="repeat(2,minmax(0,1fr))"><SelectFilter value={year} onChange={setYear} label="السنة الدراسية" icon={SchoolRounded} allLabel="كل السنوات" options={Years.map(y=>({value:y,label:y}))}/><ClassFilter classId={classId} setClassId={setClassId} academicYear={year}/></FilterCard><SectionCard title="الطلاب المشتركون" description="افتح ملف الطالب لمراجعة الأقساط أو تسجيل دفعة.">{!loading&&!actionLoading&&rows.length===0?<EmptyState icon={active?<SearchOffRounded/>:<DirectionsBusRounded/>} title={active?"لا توجد بيانات مطابقة للفلاتر":"لا توجد بيانات باص حتى الآن"} description={active?"غيّر الفلاتر أو امسحها لعرض نتائج أخرى.":"أضف أول طالب إلى خدمة الباص."} actionLabel={active?"مسح الفلاتر":undefined} onAction={active?reset:undefined}/>:<Box sx={{p:1}}><Table headers={headers} data={rows} loading={loading||actionLoading} profile={permissions?.read} deleteFn={permissions?.edit?unenroll:undefined} body={body}/>{pagination&&rows.length>0&&<PaginationControls pagination={pagination} page={page} onPageChange={setPage} limit={limit} onLimitChange={setLimit} label="عدد الطلاب"/>}</Box>}</SectionCard><EnrollDialog open={open} onClose={()=>setOpen(false)} onSubmit={enroll} loading={actionLoading} candidates={candidateOptions} plans={plans}/></Box></Container>;
};
const EnrollDialog=({open,onClose,onSubmit,loading,candidates,plans})=>{const {register,handleSubmit,reset,formState:{errors}}=useForm();useEffect(()=>{if(open)reset({studentId:"",fee:"",serviceType:"both",installmentPlanId:""})},[open,reset]);return <Dialog open={open} onClose={onClose} fullWidth maxWidth="md" PaperProps={{sx:{overflow:"hidden",borderRadius:"20px",bgcolor:"var(--color-cream)"}}}><DialogHeader icon={<DirectionsBusRounded/>} title="إضافة طالب إلى خدمة الباص" description="حدّد الطالب والرسوم ونوع الخدمة وخطة التقسيط." loading={loading} onClose={onClose}/><DialogContent sx={{...formFieldsSx,p:2}}><Box component="form" onSubmit={handleSubmit(onSubmit)} sx={{display:"grid",gridTemplateColumns:{xs:"1fr",sm:"repeat(2,minmax(0,1fr))"},gap:1.5}}><Box sx={{gridColumn:{sm:"1/-1"}}}><Select register={register} registerName="studentId" data={candidates} name="displayName" error={errors.studentId?.message} label="الطالب" required/></Box><Input register={register} registerName="fee" error={errors.fee?.message} label="رسوم الباص" required type="number" valueAsNumber/><Select register={register} registerName="serviceType" data={[{_id:"pickup",displayName:"ذهاب فقط"},{_id:"dropoff",displayName:"عودة فقط"},{_id:"both",displayName:"ذهاب وعودة"}]} name="displayName" label="نوع الخدمة" required/><Box sx={{gridColumn:{sm:"1/-1"}}}><Select register={register} registerName="installmentPlanId" data={plans} name="displayName" label="خطة التقسيط" defaultSelect="كاش بدون تقسيط"/></Box><Box sx={{gridColumn:{sm:"1/-1"}}}><FormActions loading={loading} onCancel={onClose} label="إضافة الطالب" disabled={!candidates.length}/></Box></Box></DialogContent></Dialog>};
export default BusListPage;
